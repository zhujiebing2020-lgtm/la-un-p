# 方法附录：独立阅读所需的定义

对应论文v0.3.2、研究核v0.2。此处公开配置合同，不宣称完整harness已发布。

## 输入与运行顺序

每轮输入需要事件ID、可知历史、人物初始配置、Csoc∈[0,1]、Sig∈[0,1]、Sctx≥1、信号类别q。数值来源须注明人工设定/解释器预测/经审核事件；unknown不以0.5填充。完整自然语言解释映射仍待验证。

先由外层安全路由决定是否进入数值核；E12不进入生长支撑集。随后按论文§4.2计算U/E_in/F/h/H/G，G≤阈值时保留当前S；否则对合法支撑集计算softmax并抽样。实际跳转后H按release释放。最后依据独立事件ID及方向更新Ic。表达器可提议，不能改写核已执行状态。

默认rho=0.8，beta=0.5，release=0.5，tau=0.15，信号匹配倍率1.8，身份记账幅度0.03，均为未校准的工程选择。Ic、Dc、阈值与H取[0,1]；W非负且总和>0，归一化后计算目的状态得分。

## 精确支撑集

- T_linear：E1→{E4}；E4→{E1,E6}；E6→{E4,E8}；E8→{E6,E9}；E9→{E4,E8}。当前代码对其他有效生长状态回退到{E4}，建议配置时限制在声明簇内并另报越簇初始化。
- T_tense：每个有效当前状态的过阈候选固定为{E2,E3,E7,E8,E10}，可能包含自环。
- T_erratic：候选固定为{E2,E3,E7}，可能包含自环。
- 论文§3.3主路径是示意；本表才是当前原型的候选集合。不可据示意箭头自行添加边。

## 状态与需求类别映射

| 需求类别 | 状态 |
|---|---|
| novelty | E1、E3 |
| conflict | E2、E9、E10 |
| recognition | E4、E5 |
| care | E6、E8、E11 |
| taboo | E7 |

E0无类别映射；E12是外层安全路由。缺少对应权重时该类别得分为0。同类候选同分，是本版的表示限制；autonomy不应强行等同conflict。状态词义见论文§3.5，它们不是同一层级的心理分类，也不是经验证量表。

## 事件记账

输入identity_event、identity_direction(loss/gain)、identity_amount。仅D完整核执行：gain可在不跳转时增加，loss须实际跳转；两者共享事件去重集，数值裁剪至[0,1]。同事件不可重复记账，即使达到边界也记录已处理。事件语义与真实性必须来自独立依据，不由模型自述授予。

## 可追溯与重放

至少保存输入片段/事件ID、事实与推断的区别、映射版本、状态前后、门控与候选集、随机种子及实现版本、原始生成请求/响应、行动提议和执行回执。给出模型理由不等于保存执行因果。数值核可在固定输入和随机机制下检查重放；外部模型输出复现仍受提供方及版本限制。

## 公开材料边界

仓库中的小脚本只复算门控，并非完整实现。若需独立实现完整系统，仍需自行提供安全路由、自然语言解释、人物上下文表达和动作执行器；这些接口的性能没有由本文证明。精确参数到自然语言行为的映射及学习规则仍是开放问题。
